# scripts

## link-skills.sh

Symlinks skills from this repo into `~/.claude/skills/` so Claude Code can pick them up.

```bash
# Link all skills (default behavior)
bash scripts/link-skills.sh

# Link specific skills by name
bash scripts/link-skills.sh commit handoff review-pr
```
